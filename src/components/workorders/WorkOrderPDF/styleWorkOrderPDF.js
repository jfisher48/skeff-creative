import { StyleSheet } from "@react-pdf/renderer";

const styles = StyleSheet.create({
  body: {
    margin: 35
  },
  container: {
    margin: 10,
    padding: 10,
    flex: 1,
    flexDirection: "row"
  }
});

export default styles;
